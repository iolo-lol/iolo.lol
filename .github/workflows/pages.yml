# TEMPORARY FALLBACK during cutover to Cloudflare Workers Static Assets
# (iolo.lol#20, ADR-0005). Remove this workflow and the Pages deployment once
# QA verifies the Cloudflare production surface and iolo.lol DNS serves the
# canonical domain from Cloudflare.
name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.11.0
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build the SvelteKit static site from canonical signal state
        run: pnpm --filter @iolo.lol/web build
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: apps/web/build
      - uses: actions/deploy-pages@v5
